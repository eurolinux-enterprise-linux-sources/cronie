diff -up cronie-1.4.4/src/cron.c.getpwnam cronie-1.4.4/src/cron.c
--- cronie-1.4.4/src/cron.c.getpwnam	2015-02-05 15:35:17.000000000 +0100
+++ cronie-1.4.4/src/cron.c	2015-02-05 15:41:49.244379469 +0100
@@ -530,6 +530,9 @@ static void find_jobs(int vtime, cron_db
 						job_add(e, u);	/*will add job, if it isn't in queue already for NOW. */
 				}
 			}
+			else {
+				log_it(uname, getpid(), "ERROR", "getpwnam() failed", errno);
+			}
 		}
 	}
 	if (orig_tz != NULL)
